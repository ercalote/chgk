# ЧГК - Что? Где? Когда?

Простой веб-сайт для викторины в стиле "Что? Где? Когда?" с русским интерфейсом.

## Особенности

- 👨‍💼 **Админ панель** для создания вопросов
- 🔗 **Уникальные ссылки** для каждого вопроса
- ✅ **Проверка ответов** с автоматическим поздравлением
- ⏱️ **30-секундный таймаут** после неправильного ответа
- 📱 **Оптимизировано для мобильных устройств**
- 🎨 **Тематический дизайн** в стиле "Что? Где? Когда?"
- 🇷🇺 **Полностью на русском языке**

## Технологии

- **Backend**: Python Flask (легкий, без сложного развертывания)
- **Frontend**: HTML, CSS, JavaScript (чистый, без фреймворков)
- **Хранилище**: JSON файл (не требуется база данных)

## Установка и запуск

### Требования

- Python 3.7 или выше

### Быстрый старт

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ercalote/chgk.git
cd chgk
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Запустите приложение:
```bash
python app.py
```

4. Откройте браузер и перейдите по адресу:
```
http://localhost:5000
```

## Использование

### Создание вопроса (Админ)

1. Откройте главную страницу `http://localhost:5000`
2. Введите текст вопроса
3. Введите правильный ответ (регистр не важен)
4. Нажмите "Создать вопрос"
5. Скопируйте уникальную ссылку и отправьте участникам

### Ответ на вопрос (Участник)

1. Откройте уникальную ссылку вопроса (например, `http://localhost:5000/q/abc123...`)
2. Прочитайте вопрос
3. Введите ответ в поле
4. Нажмите "Ответить"

**Правильный ответ**: Отображается поздравление 🎉

**Неправильный ответ**: Отображается ошибка ❌ и 30-секундный таймер перед следующей попыткой

## Развертывание

### На локальном сервере

Для развертывания в продакшене рекомендуется использовать Gunicorn:

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

### Автоматическое развертывание в Azure (CI/CD)

Проект настроен для автоматического развертывания в Azure Web App через GitHub Actions.

#### Настройка CI/CD пайплайна

1. **Создайте Azure Web App**:
   - Перейдите в Azure Portal
   - Создайте новый Web App с Python 3.9 runtime
   - Скачайте publish profile из настроек развертывания

2. **Настройте GitHub Secrets**:
   - Перейдите в Settings → Secrets and variables → Actions в вашем репозитории
   - Добавьте следующие secrets:
     - `AZURE_WEBAPP_NAME`: имя вашего Azure Web App
     - `AZURE_WEBAPP_PUBLISH_PROFILE`: содержимое publish profile файла

3. **Запуск деплоя**:
   - Пайплайн автоматически запускается при каждом push в ветку `mainline`
   - Процесс включает:
     - Загрузку кода репозитория
     - Установку Python 3.9
     - Установку зависимостей из `requirements.txt`
     - Автоматическое развертывание в Azure Web App

#### Мониторинг деплоя

- Статус деплоя можно отслеживать во вкладке Actions репозитория
- Azure Web App логи доступны в Azure Portal

#### Важно: Сохранение данных

Приложение использует файл `data.json` для хранения данных. По умолчанию файловая система Azure Web App является эфемерной - данные могут быть потеряны при перезапуске или повторном развертывании. Для сохранения данных рекомендуется:

- Использовать Azure Storage для постоянного хранения файлов
- Настроить Azure Database (например, Azure Cosmos DB или Azure Database for PostgreSQL)
- Включить постоянное хранилище для директории `/home` в настройках Azure App Service

### В облаке (альтернативные платформы)

Приложение также совместимо с различными платформами:

- **Heroku**: Используйте Procfile
- **PythonAnywhere**: Загрузите файлы и настройте веб-приложение Flask
- **DigitalOcean App Platform**: Развертывание через Git
- **Railway**: Автоматическое развертывание из GitHub

Пример Procfile для Heroku:
```
web: gunicorn app:app
```

## Структура проекта

```
chgk/
├── app.py                 # Основное приложение Flask
├── requirements.txt       # Python зависимости
├── data.json             # Хранилище данных (создается автоматически)
├── templates/
│   ├── admin.html        # Админ панель
│   └── question.html     # Страница вопроса
└── static/
    └── style.css         # Стили в теме ЧГК
```

## Безопасность

- Таймауты защищают от спама
- Client ID используется для отслеживания попыток
- Ответы хранятся в JSON файле на сервере

**Примечание**: Для продакшн использования рекомендуется добавить:
- Аутентификацию для админ панели
- Ограничение частоты запросов (rate limiting)
- HTTPS для защиты передачи данных

## Лицензия

MIT License