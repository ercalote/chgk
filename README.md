# ЧГК - Что? Где? Когда?

Простой веб-сайт для викторины в стиле "Что? Где? Когда?" с русским интерфейсом.

## Особенности

- 👨‍💼 **Админ панель** для создания вопросов
- 🔗 **Уникальные ссылки** для каждого вопроса с MD5 хешированием
- 📋 **Список всех вопросов** с возможностью просмотра и копирования ссылок
- ✅ **Проверка ответов** с автоматическим поздравлением
- ⏱️ **30-секундный таймаут** после неправильного ответа
- 📱 **Оптимизировано для мобильных устройств**
- 🎨 **Тематический дизайн** в стиле "Что? Где? Когда?"
- 🇷🇺 **Полностью на русском языке**
- 🌐 **Хостинг на GitHub Pages** - статический сайт без сервера
- 🔑 **MD5 хеш-идентификаторы** для уникальных и предсказуемых ID вопросов

## Технологии

- **Frontend**: HTML, CSS, JavaScript (чистый, без фреймворков)
- **Backend**: Flask (Python) для API и хранения данных
- **Хранилище**: JSON файл на сервере (синхронизация между устройствами)

## Использование

Приложение поддерживает два режима работы:

### Режим 1: С Flask сервером (рекомендуется)

**Преимущества**: Полная функциональность, вопросы доступны с любого устройства

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ercalote/chgk.git
cd chgk
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Запустите Flask сервер:
```bash
python app.py
```

4. Откройте браузер и перейдите по адресу:
```
http://localhost:5000
```

### Режим 2: GitHub Pages (статический хостинг)

**Ограничения**: Администратор должен добавлять вопросы в файл `questions.json` и делать commit

**Настройка:**
1. Форкните репозиторий
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source** выберите ветку `main`
4. Сайт будет доступен по адресу `https://<username>.github.io/chgk/`

**Добавление вопросов:**

**Вариант 1: Используя helper script (рекомендуется)**
```bash
python add_question.py
# Следуйте инструкциям, затем:
git add questions.json
git commit -m "Добавлен новый вопрос"
git push
```

**Вариант 2: Вручную редактируя questions.json**
1. Откройте файл `questions.json` в репозитории
2. Добавьте новый вопрос в формате:
```json
{
  "questions": {
    "md5_hash_of_question": {
      "question": "Текст вопроса",
      "answer": "правильный ответ",
      "created_at": "2026-01-29T00:00:00.000000"
    }
  }
}
```
3. Сделайте commit и push
4. Уникальная ссылка: `https://<username>.github.io/chgk/question.html?id=md5_hash_of_question`
5. Просмотрите все вопросы на странице: `https://<username>.github.io/chgk/questions.html`

**Примечание:** ID вопроса - это MD5 хеш текста вопроса. Вы можете сгенерировать его используя `add_question.py` скрипт, который автоматически вычисляет MD5 хеш.

**Примечание**: В режиме GitHub Pages вопросы хранятся в файле `questions.json` и доступны всем по уникальным ссылкам. Администратор также может создавать вопросы через веб-интерфейс, но они будут сохранены только в localStorage его браузера.

## Как это работает

### Создание вопроса (Админ)

**С Flask сервером:**
1. Откройте главную страницу (http://localhost:5000 или ваш сервер)
2. Введите текст вопроса
3. Введите правильный ответ (регистр не важен)
4. Нажмите "Создать вопрос"
5. Скопируйте уникальную ссылку и отправьте участникам
6. Просмотрите все вопросы на странице `/questions.html`

**Примечание:** ID вопроса генерируется как MD5 хеш текста вопроса, что означает:
- Один и тот же вопрос всегда будет иметь один и тот же ID
- Попытка создать дубликат вопроса будет отклонена
- ID предсказуемый и детерминированный

**На GitHub Pages:**
1. **Вариант А (рекомендуется)**: Добавьте вопрос в файл `questions.json`:
   - Откройте `questions.json` в редакторе
   - Добавьте вопрос с уникальным ID
   - Сделайте commit и push
   - Поделитесь ссылкой: `https://username.github.io/chgk/question.html?id=your_question_id`

2. **Вариант Б**: Через веб-интерфейс (локальное хранение):
   - Откройте главную страницу (https://username.github.io/chgk/)
   - Введите текст вопроса и ответ
   - Нажмите "Создать вопрос"
   - Скопируйте ссылку (вопрос сохранится в вашем браузере)
   - ⚠️ Вопрос будет доступен только если участники используют ту же ссылку

### Ответ на вопрос (Участник)

1. Откройте уникальную ссылку вопроса
   - С сервером: `http://localhost:5000/q/123e4567...`
   - GitHub Pages: `https://username.github.io/chgk/question.html?id=q_123...`
2. Прочитайте вопрос
3. Введите ответ в поле
4. Нажмите "Ответить"

**Правильный ответ**: Отображается поздравление 🎉

**Неправильный ответ**: Отображается ошибка ❌ и 30-секундный таймер перед следующей попыткой

### Хранение данных

**С Flask сервером:**
- Вопросы и ответы сохраняются в **JSON файле** на сервере (data.json)
- Данные доступны с любого устройства, подключенного к серверу
- Уникальные ссылки работают на всех устройствах
- Попытки ответов отслеживаются по ID клиента для таймаута

**На GitHub Pages:**
- **Вопросы из questions.json**: Доступны всем по уникальным ссылкам (хранятся в репозитории)
- **Вопросы через интерфейс**: Сохраняются в localStorage браузера создателя
- Попытки ответов хранятся локально для каждого участника
- Таймауты работают на уровне браузера каждого участника

## Развертывание

### Развертывание на сервере

1. Установите Python 3 и pip на вашем сервере
2. Клонируйте репозиторий:
```bash
git clone https://github.com/ercalote/chgk.git
cd chgk
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Запустите приложение:
```bash
# Для разработки
python app.py

# Для продакшена (с Gunicorn)
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

### Развертывание с Docker (рекомендуется)

Создайте Dockerfile:
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 5000
CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:5000", "app:app"]
```

Запустите контейнер:
```bash
docker build -t chgk .
docker run -p 5000:5000 -v $(pwd)/data.json:/app/data.json chgk
```

## API Endpoints

Приложение предоставляет следующие API endpoints:

### POST /api/question
Создать новый вопрос
```json
{
  "question": "Текст вопроса",
  "answer": "правильный ответ",
  "success_image": "https://example.com/image.jpg" // опционально
}
```
Возвращает:
```json
{
  "id": "md5_hash",
  "url": "/q/md5_hash"
}
```

### GET /api/questions
Получить список всех вопросов
Возвращает:
```json
{
  "questions": [
    {
      "id": "md5_hash",
      "question": "Текст вопроса",
      "url": "/q/md5_hash",
      "created_at": "2026-01-29T00:00:00"
    }
  ],
  "total": 10
}
```

### GET /api/question/:id
Получить вопрос по ID (без ответа)

### POST /api/answer/:id
Проверить ответ на вопрос

## Структура проекта

```
chgk/
├── index.html            # Админ панель (главная страница для GitHub Pages)
├── question.html         # Страница для отображения вопросов (GitHub Pages)
├── questions.html        # Страница со списком всех вопросов
├── static/
│   └── style.css         # Стили в теме ЧГК
├── templates/            # Flask шаблоны
│   ├── admin.html        # Flask-версия админ панели
│   └── question.html     # Flask-версия страницы вопроса
├── app.py                # Flask приложение (API сервер)
├── add_question.py       # Скрипт для добавления вопросов в questions.json
├── requirements.txt      # Python зависимости
├── questions.json        # Хранилище вопросов для GitHub Pages
└── data.json             # Хранилище вопросов для Flask (создается автоматически)
```

## Особенности и ограничения

### С Flask сервером

**Преимущества:**
- ✅ Синхронизация между устройствами
- ✅ Вопросы доступны с любого устройства
- ✅ Уникальные ссылки работают везде
- ✅ Централизованное хранение данных
- ✅ Простое развертывание

**Требования:**
- ⚠️ Требуется запущенный Flask сервер
- ⚠️ Необходим доступ к серверу для всех участников
- ⚠️ Данные хранятся в JSON файле (для продакшена рекомендуется база данных)

### На GitHub Pages

**Преимущества:**
- ✅ Бесплатный хостинг
- ✅ Не требует сервера
- ✅ Мгновенная загрузка
- ✅ Работает оффлайн (после первой загрузки)

**Ограничения:**
- ⚠️ Вопросы добавляются через commit в `questions.json`
- ⚠️ Или хранятся в localStorage браузера (не синхронизируются)
- ⚠️ Нет автоматической админ-панели для управления вопросами
- ⚠️ Очистка данных браузера удалит локальные вопросы

### Безопасность

**С Flask сервером:**
- Client ID используется для отслеживания попыток
- 30-секундный таймаут хранится на сервере
- Валидация входных данных на сервере
- Защита от XSS атак при валидации URL изображений

**На GitHub Pages:**
- Client ID используется для отслеживания попыток локально
- 30-секундный таймаут хранится в браузере
- Все данные обрабатываются на клиентской стороне
- HTTPS автоматически используется на GitHub Pages

## Лицензия

MIT License